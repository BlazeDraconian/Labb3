<UserControl x:Class="Labb3.Views.ConfigurationView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Labb3.Views" xmlns:viewmodels="clr-namespace:Labb3.ViewModels" d:DataContext="{d:DesignInstance Type=viewmodels:ConfigurationViewModel}"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800"
             Background="White">
    <!--DataContext="{Binding ConfigurationViewModel}"> --> 


    <Grid Margin="10,0,0,0">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="2*"/>
            <ColumnDefinition Width="2*"/>
        </Grid.ColumnDefinitions>

        <Border Grid.Column="0">
            <StackPanel>

                <TextBlock
                    Text="{Binding ActivePack.Name}"
                    FontWeight="Bold"
                    FontSize="12"
                    Margin="0,30,0,0"/>
                <ListBox
                    ItemsSource="{Binding ActivePack.Questions}" 
                    SelectedItem="{Binding ActivePack.SelectedQuestion}" 
                    Margin="5"
                    FontSize="14"
                    ScrollViewer.VerticalScrollBarVisibility="Visible"
                    Height="395">

                    <ListBox.ItemTemplate>
                    <DataTemplate>
                        <TextBlock Text="{Binding Query}" />
                    </DataTemplate>
                    </ListBox.ItemTemplate>
                    </ListBox>


            </StackPanel>
        </Border>

        <Border Grid.Column="1">
            <StackPanel>
                <TextBlock Text="Question:" FontWeight="Bold" Margin="0,0,0,5"/>
                <TextBox Text="{Binding ActivePack.SelectedQuestion.Query, UpdateSourceTrigger=PropertyChanged}" Height="100" />


                <TextBlock Text="Correct Answer:" FontWeight="Bold" Margin="0,15,0,0"/>
                <TextBox Text="{Binding ActivePack.SelectedQuestion.CorrectAnswer, UpdateSourceTrigger=PropertyChanged}" Margin="0,5,0,0"/>

                <TextBlock Text="First Incorrect Answer:" FontWeight="Bold"/>
                <TextBox Text="{Binding ActivePack.SelectedQuestion.IncorrectAnswers[0], UpdateSourceTrigger=PropertyChanged}" Margin="0,5,0,0"/>

                <TextBlock Text="Second Incorrect Answer:" FontWeight="Bold"/>
                <TextBox Text="{Binding ActivePack.SelectedQuestion.IncorrectAnswers[1], UpdateSourceTrigger=PropertyChanged}" Margin="0,5,0,0"/>

                <TextBlock Text="Third Incorrect Answer:" FontWeight="Bold"/>
                <TextBox Text="{Binding ActivePack.SelectedQuestion.IncorrectAnswers[2], UpdateSourceTrigger=PropertyChanged}" Margin="0,5,0,0"/>

            </StackPanel>
            
        </Border>

    </Grid>

</UserControl>

