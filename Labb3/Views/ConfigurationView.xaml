<UserControl x:Class="Labb3.Views.ConfigurationView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Labb3.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800"
             Background="White"
             DataContext="{Binding ConfigurationViewModel}">


    <Grid Margin="10,0,0,0">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="2*"/>
            <ColumnDefinition Width="2*"/>
        </Grid.ColumnDefinitions>

        <Border Grid.Column="0">
            <StackPanel>

                <TextBlock
                    DataContext="{Binding MainWindowViewModels}"
                    Text="{Binding ActivePack}"
                    FontWeight="Bold"
                    FontSize="12"
                    Margin="0,30,0,0"/>
                <ListBox 
                    DataContext="{Binding MainWindowViewModels}"
                    ItemsSource="{Binding ActivePack}" 
                    SelectedItem="{Binding QuestionPack}" 
                    Margin="5"
                    ScrollViewer.VerticalScrollBarVisibility="Visible"
                    Height="395"/>


            </StackPanel>
        </Border>

        <Border Grid.Column="1">
            <StackPanel DataContext="{Binding Question}">
                <TextBlock Text="Question:" FontWeight="Bold" Margin="0,0,0,5"/>
                <TextBox Text="{Binding ActivePack.Query}" Height="100" />


                <TextBlock Text="Correct Answer:" FontWeight="Bold" Margin="0,15,0,0"/>
                <TextBox Text="{Binding ActivePack.CorrectAnswer}" Margin="0,5,0,0"/>

                <TextBlock Text="First Incorrect Answer:" FontWeight="Bold"/>
                <TextBox Text="{Binding ActivePack.IncorrectAnswer1}" Margin="0,5,0,0"/>

                <TextBlock Text="Second Incorrect Answer:" FontWeight="Bold"/>
                <TextBox Text="{Binding ActivePack.IncorrectAnswer2}" Margin="0,5,0,0"/>

                <TextBlock Text="Third Incorrect Answer:" FontWeight="Bold"/>
                <TextBox Text="{Binding ActivePack. SelectedQuestion.IncorrectAnswer3}" Margin="0,5,0,0"/>

            </StackPanel>


        </Border>
    </Grid>

</UserControl>

